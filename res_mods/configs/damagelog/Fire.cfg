{
	/**
	 *   ~ Settings pop-up message in case of fire. ~
	 */
	"FIRE" : {
		"enable" : false, // Element mapping (true, false).
		"animation" : true, // Map animation (true, false).
		"showFireIcon" : true, // Display standard fire icons (true, false).
		"showPostmortem" : true, // Display when the camera is switched to another player in the post-mortem mode (true, false).
		"position" : {
			"x" : -50, // Axis coordinates of Х.
			"y" : -350, // Axis coordinates of Y.
			"align" : {
				"x" : "center", // Alignment element axis Х (left, center, right).
				"y" : "bottom" // Alignment element axis Y (top, center, bottom).
			}
		},
		"size" : {
			"width" : 100, // Element width.
			"height" : 25 // Element height.
		},
		"background" : {
			"image" : "default", // Path to the background image (default - Default).
			"alpha" : 0, // Transparency of the background image (0-100).
			"width" : 120, // Width of the background image.
			"height" : 35, // Height of the background image.
			"offset" : {
				"x" : -10, // Offset of the background image relative to the text field on the axisХ.
				"y" : -5 // Offset of the background image relative to the text field on the axis Y.
			}
		},
		"shadow" : {
			"alpha" : 90, // Transparency of the shadow text.
			"color" : "#FF0000", // Color of the text.
			"angle" : 90, // Angle of the shadow offset text.
			"strength" : 300, // Intensity of the shadow of the text.
			"distance" : 0, // Text shadow offset distance.
			"size" : 7 // Text size.
		},
		"format" : {
			"text" : "<p align='center'><font size='18'><b>Fire!</font></b></p>" // Pop-up text messages.
		},
		"debug" : false // Display element boundaries to fine-tune (true, false).
	}
}
